# ---------------------------------------------------------------------------
# ForgeML — runtime requirements for the FULL training environment.
#
# This file is what you install on a GPU machine (Databricks GPU cluster,
# Colab, RunPod, your own box). For CI / API-only installs use:
#
#   pip install -e ".[dev]"        # light: no torch
#   pip install -e ".[api]"        # serving only
#   pip install -e ".[train]"      # everything below
#
# Pinned to lower bounds, not exact versions, because Databricks runtimes ship
# their own torch build and exact pins fight with it.
# ---------------------------------------------------------------------------

# --- core -------------------------------------------------------------------
pydantic>=2.6
pyyaml>=6.0
numpy>=1.26
pandas>=2.1
rich>=13.7
typer>=0.12

# --- experiment tracking ----------------------------------------------------
mlflow>=2.16

# --- modelling --------------------------------------------------------------
torch>=2.3
transformers>=4.44
datasets>=2.20
accelerate>=0.33
peft>=0.12
trl>=0.9
sentencepiece>=0.2

# 4-bit / 8-bit quantization. No macOS wheels — QLoRA needs CUDA.
bitsandbytes>=0.43; platform_system != "Darwin"

# --- evaluation -------------------------------------------------------------
evaluate>=0.4
rouge-score>=0.1.2

# --- serving ----------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
